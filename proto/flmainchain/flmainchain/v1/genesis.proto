syntax = "proto3";
package flmainchain.flmainchain.v1;

import "amino/amino.proto";
import "flmainchain/flmainchain/v1/params.proto";
import "gogoproto/gogo.proto";

option go_package = "flmainchain/x/flmainchain/types";

// GenesisState defines the flmainchain module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
